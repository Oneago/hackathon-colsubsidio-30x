# API — FastAPI + SQLAlchemy + Alembic (Postgres vía psycopg v3)
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
alembic==1.14.0
pydantic-settings==2.7.0

# Seguridad / auth (se usa desde la Fase 1)
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
pyjwt==2.10.1
python-multipart==0.0.20

# Integraciones y pruebas (se usan desde la Fase 1)
httpx==0.28.1
pytest==8.3.4
